apiVersion: v1
kind: Service
metadata:
  name: kot-server
  labels:
    app: kot-server
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: kot-server
