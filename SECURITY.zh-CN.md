# 安全策略

## 支持范围

安全修复面向当前开发分支和最新发布的项目状态。当前仓库不维护多个并行支持的发布分支。

- 支持范围：当前开发分支。
- 不支持范围：历史提交和未发布 Fork。

## 漏洞报告

请优先通过代码托管平台提供的私密安全报告渠道向维护者报告疑似漏洞。若平台没有私密报告能力，请直接联系维护者，不要创建公开 Issue。

不要在公开 Issue、Pull Request、提交记录或评论中包含漏洞利用代码、密钥、个人数据、access token 或生产环境地址。

报告中建议包含以下信息：

- 受影响组件和影响范围的简要说明。
- 复现步骤或最小化 PoC。
- 受影响版本、提交 ID 或部署条件。
- 已知的缓解方案或修复建议。

维护者会确认收到报告，评估可复现性和严重程度，在需要时协调修复，并在具备处置计划后披露处理结果。

## 当前安全基线

- 认证使用 Micronaut Security Bearer Token 与 JWT access token。
- 会话状态保存在 Redis 中；生产 Redis 必须部署在私有网络并启用认证。
- 管理端授权通过 RBAC 与 `@RequirePermission` 在服务端强制执行；前端菜单可见性不是授权控制。
- `super_admin` 是数据库维护的引导角色，不能通过角色管理 API 暴露。
- 密码通过 framework 的 `PasswordEncoder` 抽象存储，支持 Argon2id、BCrypt 和 PBKDF2-HMAC-SHA256。
- 当前有效权限直接从持久化层解析，因此 RBAC 修改会在下一次请求生效。JWT 仅承载身份和会话信息，不能作为当前权限的唯一来源。

## 部署要求

- 在源代码之外配置随机 `JWT_SECRET`，长度至少为 32 字节；不得使用仓库中的默认兜底值部署生产环境。
- 使用部署密钥或环境专属配置提供 `R2DBC_URL`、`R2DBC_USER`、`R2DBC_PASSWORD` 和 `REDIS_URI`。
- 公网入口、管理端客户端和外部菜单目标必须使用 TLS/HTTPS。
- PostgreSQL 和 Redis 的网络访问仅开放给应用工作负载和授权运维人员。
- 发生疑似泄露后应轮换凭据和 JWT 签名密钥。除非实现了协调过渡，轮换会使已有会话失效。
- Swagger/OpenAPI 端点不应在无意情况下对公网开放；是否公开必须由部署层的访问控制明确决定。

## 安全开发要求

- 不得提交凭据、token、私钥、生产数据库导出文件或包含秘密信息的本地配置。
- 日志不得记录密码、Authorization 请求头、JWT、refresh token、session ID 或含凭据的连接字符串。
- 使用参数化的 Micronaut Data/R2DBC 访问，不得用不可信字符串拼接 SQL。
- 校验菜单中的外部 URL。外部和 iframe 目标必须使用 HTTPS，并由消费端通过允许列表限制来源。
- `sql/kot_cloud_system.sql` 是破坏性初始化脚本，会删除并重建系统表，执行前必须审查。
- 依赖更新必须经过审慎验证，未经兼容性验证不得顺手升级版本。

## 不在范围内的事项

- 本地环境修改、未经审查的 Fork 或不再支持的历史版本中的漏洞。
- 在已有数据的数据库中故意执行破坏性初始化 SQL 导致的可用性问题。
- 当前 Docker Compose 骨架未提供、但应由生产部署层负责的安全能力，例如 TLS 终止、密钥管理、数据库供应和网络策略。
